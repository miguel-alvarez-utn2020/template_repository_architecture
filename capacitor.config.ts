import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'template_arquitecture_repository',
  webDir: 'www'
};

export default config;
